# FAECO 项目交接记录（2026-08-13）

> 本记录回看会话 `019fefa2-b03f-75c2-8990-72f33645e47c`（「严格审稿并迭代论文」，2026-08-11 ~ 08-13，共 61 轮），补齐其最后一轮「准备交接 做好记录」因 API 401 失败而未完成的交接。

## 一、本阶段做了什么（论文严格复审与迭代）

### 1. 多技能严格复审 + 实验证据盘点（08-11）
- 用 academic / nature / 用户写作风格三种 skill 角度对 `faeco_paper_jcad.pdf` 严格审稿，综合意见后逐轮修改。
- 实验证据逐项盘点，结论是「不是实验没做，而是已有实验分散在多个批次、论文把不同配置混写」，先做数据对齐、不盲目重跑：
  - 20 轮混合/纯 G/纯 B 已完成（混合平均 +1.074 ns，纯 G +0.161 ns）。
  - 8 电路真实外层割—反馈 8/8 改善，STA 合计 27 次。
  - 反馈 ON/OFF 消融完成；SPEF 扫描完成（s382/b18）；Hold 补充实验 7/8 改善。
  - P&R 有网表/manifest/脚本，但本地缺 pr_run.log / DEF / ODB / SPEF / DRC 等结果文件，需追回外部日志。
  - 关键口径：27 vs 110 次 STA 不能直接写「同一候选空间下无损减 75.5%」，需逐电路对齐。
- 生成实验证据盘点 `experiment_evidence_inventory_round5.md` 与数据字典 `experiment_data_dictionary_round5.md`。

### 2. 主结果口径核实（08-11）
- ITC-99 19 个 JSON 逐项核对：1693 次 STA，18/19 改善，b06 失败；P&R 补做 16 预布局网表审计与表格一致；b18/b19 明确为独立补充实验不计入 19 电路统计。
- 主图改为直接读实验 JSON，移除手填主结果数组；代理评分仅保留 opt-in 审计链路，不写为主结果贡献。
- 最终验证：268 tests passed；PDF 11 页；无 Overfull / Fatal / 未定义引用。

### 3. 图表重画 + 排版重排（08-11）
- 重画 ISCAS89 / ITC-99 / 基线 / SPEF 图，新增 P&R 配对差值图。
- 全文排版重排：双栏结构与页边距、压缩首页空白、浮动体边界、附录满宽单栏、参考文献独立分页。11 页 A4 编译通过。

### 4. 摘要与实验设置压缩（08-11）
- 摘要结构收敛为「背景问题 → 方法机制 → 验证流程 → 综合结果 → 结论与边界」，中文约 295 字 / 英文约 179 词。
- 实验设置压缩为「环境与数据集」「配置与参数」两段，保留全部复现参数与 27/110 审计说明。

### 5. 口径 / 结构 / 术语修正（08-12）
- ITC-99 口径修正：**19/19 不劣于基线（18 严格改善 + b06 持平 −0.56 ns）**，不再写「18/19 改善」。
- 算法 1 简化为流程示意；表 1 改为四类方法并列对比（结构重映射 / 逻辑补丁构造 / B-G 物理优化 / FAECO）。
- 补齐术语首次定义：SKY130 HD / ISCAS89 / ITC-99 / PicoRV32 / SPEF / Liberty。
- 修复根目录 PDF 未同步覆盖的问题（manuscript 与根目录 PDF SHA256 一致）。

### 6. 机制图重绘（08-12）
- 图 2（候选生成）、图 3（失效反馈闭环）经多轮 inpainting 局部编辑 + 最终低饱和论文风整图重画。
- 删除代理评分 `score(p)` / `Δtiming` / 未定义权重 `λ_e`，修正 F1–F6 归属与参数更新表、反馈回路方向，ABC CEC 改为旁路。
- 图 3 替换为 `10_34_04.png` 并改为跨双栏 `figure*`。

### 7. 结论 / 表1 / 保护性描述（08-12）
- 结论按「机制与问题 / 结果与效果 / 适用边界」三问重写并独立成页。
- 表 1 压缩为「方法—主要优势—适用问题」三列。
- 清理保护性/防御性表达（「不声称 / 不能归因于 / 不等同于」等），边界改为直接陈述。

### 8. 数据图与 Nature 预检（08-13）
- 重绘图 6（扩大并平衡 b–d 面板）、图 8（改为各策略相对基线的 WNS 改善量）。
- Nature 预检 18/18 通过；摘要首次写「Failure-Aware ECO（FAECO）」全称。

## 二、当前状态（截至 2026-08-13）

- **论文 PDF**：`paper/zh/faeco_paper.pdf` == `paper/zh/manuscript/faeco_paper_jcad.pdf`（12 页 A4，约 4.8 MB，SHA256 一致）。
- **论文源文件**：`paper/zh/manuscript/faeco_paper_jcad.tex`（52.9 KB）；旧 `faeco_paper.tex` 已删除。
- **编译状态**：latexmk 12 页，0 Error / 0 Overfull hbox / 0 undefined reference。
- **Nature 预检**：18/18 pass。
- **当前引用 4 张图**（位于 `paper/zh/figures/`）：
  - `fig3_method_flow_new.png`（图 1 总览）
  - `fig_cut_generation_new.png`（图 2 候选生成）
  - `fig_feedback_loop_new.png`（图 3 失效反馈闭环）
  - `fig_spef_gate_new.png`（图 4 SPEF 门控）
- **Git**：分支 `main`，最新提交 `2fad9cd`，远程 `https://github.com/Toylog123/FAECO.git`；**当前有 314 个未提交路径**（含论文、文档、实验产物）。

## 三、待决策 / 未完成事项

1. **机制图拆分方案（最重要待决策）**：会话第 57–58 轮提出把 4 张大机制图拆为「1 张总览图 + 3 张局部机制小图」（总览 / 加权割 / 失效反馈 / SPEF 门控各管一个问题），已生成 4 张图的完整重绘提示词（含统一风格前缀与负面提示词），但**尚未应用**。当前论文仍使用上面 4 张现有图。
2. **P&R 外部日志追回**：本地缺 `pr_run.log` / `DEF` / `ODB` / `SPEF` / `DRC`，需找回或重新生成以完善物理证据链。
3. **投稿收尾**：上传复现实验包到公开仓库 + 补真实 DOI / accession。

## 四、下一步建议

1. 先由用户决定是否执行「机制图拆分」——若执行，按会话第 58 轮已备提示词逐张重绘并替换；若不执行，维持现有 4 图。
2. `git add -A && git commit` 提交本阶段论文与文档改动，然后 `git push origin main`。
3. 追回 P&R 外部日志，补齐物理验证证据。
4. 投稿前完成复现包上传与 DOI/accession 回填。