# Round 4 编辑决定与修订清单

## 决定

Major Revision。修订目标不是增加宣传性表述，而是让论文标题、算法、代码、实验指标和限制条件形成可审计的一致链条。

## 必须完成

- [ ] 重写算法 1：候选排序使用 STA 前可计算的静态代理；OpenSTA 测量只在排序后执行。
- [ ] 删除或限定顺序真实 STA 主实验中的 ABC CEC/SEC 表述；说明当前脚本使用 `real_wns_placeholder`，并给出 DFF/状态语义边界。
- [ ] 统一接受条件：setup 模式为 WNS 严格改善；TNS 仅在 WNS 相等时作为次级决策，不能与问题定义混写。
- [ ] 补全 F3/F4/F5/F6 阈值、`lambda_e` 定义、候选数量和实际终止条件。
- [ ] 把 27 次 STA 与 20 轮收敛对比拆成不同配置，取消不匹配配置下的 99.1% 强比较，或明确比较范围仅为 ideal-net STA 调用。
- [ ] 降低 SPEF 门控和“物理稳健”结论，明确当前实验不能估计召回率，真实 P&R 结果单独报告。
- [ ] 收窄“跨基准泛化”“端到端闭环”“逻辑重综合”等表述，改为当前实现和数据实际支持的范围。
- [ ] 统一相关工作中对已有方法的概括，避免“全部先构造后验证”等不可证断言。
- [ ] 修正编译警告中可直接修复的 `ctex` 重复加载和无效 `subfloat` caption 设置，并重新渲染检查版面。

## 复审门槛

下一轮复审需要逐项回答上述清单。若仍存在 P0/P1 事实性矛盾，不能接受；仅保留不影响结论的措辞、排版或补充实验建议时，才可进入终审。

